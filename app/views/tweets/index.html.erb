<div class="header">
  <div class="pets">
    愛ペットのヘルスケア
  </div>
  <div class="pets" >
    <% if user_signed_in? %>
      <li><%= link_to current_user.pets_name, "#", class: "user-nickname" %></li>
      <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "logout" %></li>
      <li><%= link_to '記録する', new_tweet_path, class: "tweet" %></li>
    <% else %>
      <li><%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
      <li><%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
    <% end %>
  </div>
</div>

<div class="top">
  <h1>TOP PAGE</h1>
  <div class="contents">
    <% @tweets.each do |tweet| %>
      <div class="tweet">
        <p><%= tweet.created_at %></p>
        <span class="name">
          <%= tweet.id %>
        </span>
      </div>
      <li><%= link_to '詳細', tweet_path(tweet.id), class: "show" %></li>
      <li><%= link_to '削除', tweet_path(tweet.id), method: :delete %></li>
    <% end %>
  </div>
</div>